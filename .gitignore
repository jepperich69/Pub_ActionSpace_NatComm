# Microdata — never commit
*.accdb
*.mdb

# RDS files inside results/ are either kernel copies or reproducible intermediates.
# The canonical kernel RDS files live in data/kernels/ and are committed there.
results/**/*.rds

# R session artifacts
Rplots.pdf
.Rhistory
.RData
.Rproj.user/
*.Rproj.user

# OS
.DS_Store
Thumbs.db
