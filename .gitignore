# Microdata — never commit
*.accdb
*.mdb

# RDS files inside results/ are either kernel copies or reproducible intermediates.
# The canonical kernel RDS files live in data/kernels/ and are committed there.
results/**/*.rds

# R session artifacts
Rplots.pdf
.Rhistory
.RData
.Rproj.user/
*.Rproj.user

# OS
.DS_Store
Thumbs.db

# Session-level microdata grid: 6.4M rows keyed by SessionId with exact age,
# sex, municipality and car ownership. Never publish. See FINDINGS.md.
grid_mv_step2.rds
**/grid_mv_step2.rds
